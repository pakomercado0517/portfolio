import React from "react";
import { db } from "../firebase";
import { addDoc, collection } from "firebase/firestore";

function CreateCost() {
  return <div>CreateCost</div>;
}

export default CreateCost;
